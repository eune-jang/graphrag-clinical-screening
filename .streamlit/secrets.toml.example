# Template for Streamlit secrets. Copy to `.streamlit/secrets.toml`
# (which is .gitignored) for local development. Set the actual values via
# the Streamlit Community Cloud dashboard for production deployment.

# Single shared password — gates URL access for both annotators.
# Pick something not in common breach lists. Communicate out-of-band.
SHARED_PASSWORD = "12345678"
